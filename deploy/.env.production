# ── PostgreSQL (local sur le serveur Plesk) ──────────────
DB_HOST=localhost
DB_PORT=5434
DB_USER=saas_prod
DB_PASSWORD=DXTezlKModYLtLaoAEdlahqo
DB_NAME=saas_crm

# ── JWT ─────────────────────────────────────
# Generer avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_ME_GENERATE_RANDOM_STRING
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# ── Encryption (cles API CRM) ──────────────
# Generer avec: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_MASTER_KEY=CHANGE_ME_GENERATE_64_HEX_CHARS

# ── Ollama (local sur le serveur Plesk) ────────────────
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3:8b-instruct-q4_K_M

# ── ElevenLabs (TTS vocal) ──────────────────
ELEVENLABS_API_KEY=53129e782199def2b39df37f0360b827eae787e668912e58a1378dc0e49be36b
ELEVENLABS_VOICE_ID=

# ── Server ──────────────────────────────────
PORT=3000
NODE_ENV=production
